import { SubTutorSearchPipe } from './sub-tutor-search.pipe';

describe('SubTutorSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SubTutorSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
